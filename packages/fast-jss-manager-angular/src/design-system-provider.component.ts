/**
 * The DesignSystemProvider is an angular component designed to provide design-system configurations to the
 * JSSManager.
 */

import { Component, Input } from '@angular/core';

/**
 * Component definition
 */
@Component({
    selector: 'design-system-provider',
    template: '{{ text }}'
})

/**
 * Class definition
 */
export class DesignSystemProviderComponent {
}